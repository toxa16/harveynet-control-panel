{
  "name": "harveynet-control-panel",
  "version": "0.0.4",
  "private": true,
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "express": "^4.17.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-redux": "^7.1.3",
    "react-scripts": "3.2.0",
    "redux": "^4.0.4",
    "redux-saga": "^1.1.3",
    "serve": "^11.2.0"
  },
  "scripts": {
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:server": "jest ./server --watch",
    "test:server:once": "jest ./server",
    "test:unit": "react-scripts test",
    "test:unit:once": "react-scripts test --watchAll=false",
    "test:e2e": "npm run cypress:open",
    "test:e2e:once": "start-server-and-test start http://localhost:3000 cypress:run",
    "test": "npm run test:unit:once && npm run test:server:once && npm run test:e2e:once",
    "start": "node ./server/index.js",
    "heroku:start": "serve ./build -l $PORT"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "bootstrap": "^4.3.1",
    "cypress": "^3.6.1",
    "jest": "^24.9.0",
    "start-server-and-test": "^1.10.6",
    "supertest": "^4.0.2"
  }
}
